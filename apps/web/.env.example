# Copy to apps/web/.env (gitignored) to override the Vite dev proxy target.
# Vite proxies /api and /ws to the API running on this port.
# Default is 8080; set 8099 when another process owns 8080 on this machine.
VITE_API_PROXY_PORT=8080