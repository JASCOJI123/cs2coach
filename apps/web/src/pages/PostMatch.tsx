import{useEffect,useState}from'react';import{api}from'../lib/api';import{navigate}from'../App';import{LanguageSwitcher,useI18n}from'../lib/i18n';import type{PostMatchAnalysis}from'../lib/types';interface Props{faceitMatchId:string}
export default function PostMatch({faceitMatchId}:Props){const{t}=useI18n();const[analysis,setAnalysis]=useState<PostMatchAnalysis|null>(null),[loading,setLoading]=useState(true),[generating,setGenerating]=useState(false),[error,setError]=useState<string|null>(null);const load=async()=>{try{setAnalysis(await api.getMatchAnalysis(faceitMatchId));setError(null)}catch(err){setError((err as Error).message)}finally{setLoading(false)}};useEffect(()=>{void load()},[faceitMatchId]);const generate=async()=>{if(generating)return;setGenerating(true);setError(null);try{setAnalysis(await api.generateMatchAnalysis(faceitMatchId))}catch(err){setError((err as Error).message)}finally{setGenerating(false)}};if(loading)return <main className="panel command-center"><header className="command-topbar"><div className="brand-lockup"><div className="brand-mark">C</div><div><strong>CS2<span>COACH</span></strong><small>AI ANALYSIS</small></div></div><LanguageSwitcher/><button className="back" onClick={()=>navigate('matches')}>‹</button></header><div className="waiting-card"><div className="spinner"/><h2>{t('analyzing')}</h2><p className="muted">{t('roundSignals')}</p></div></main>;return <main className="panel command-center"><header className="command-topbar"><div className="brand-lockup"><div className="brand-mark">C</div><div><strong>CS2<span>COACH</span></strong><small>POST-MATCH INTELLIGENCE</small></div></div><LanguageSwitcher/><button className="back" onClick={()=>navigate('matches')}>‹</button></header>{error&&<p className="error-banner">{error}</p>}{!analysis?<section className="hero-card disconnected-hero"><div className="hero-grid"/><div className="hero-copy"><span className="eyebrow">{t('matchComplete')}</span><h1>{t('turnData')}<br/><em>{t('intoSkill')}</em></h1><p>{t('reviewPerformance')}</p><button className="primary hero-cta" onClick={()=>void generate()} disabled={generating}>{generating?t('generating'):t('generate')}</button></div></section>:<><section className="card analysis-hero"><span className="eyebrow">{t('performanceScore')}</span><div className="analysis-big"><strong>{Math.round(Object.values(analysis.overallScore).reduce((a,b)=>a+b,0)/8)}</strong><small>/100</small></div><span className="elo-delta">{analysis.source==='groq'?t('aiModel'):t('ruleModel')}</span></section><section className="card"><div className="section-heading"><div><span className="eyebrow">{t('skillBreakdown')}</span><h2>{t('performance')}</h2></div></div><div className="score-grid">{Object.entries(analysis.overallScore).map(([key,value])=><div className="score-item" key={key}><span>{key.replace(/([A-Z])/g,' $1')}</span><strong>{value}</strong></div>)}</div></section><section className="card"><span className="eyebrow">{t('roundReview')}</span><div className="analysis-block"><b>{t('bestRound')}</b><p>{analysis.bestRound?`#${analysis.bestRound.roundNumber} — ${analysis.bestRound.reason}`:t('noData')}</p></div><div className="analysis-block"><b>{t('worstRound')}</b><p>{analysis.worstRound?`#${analysis.worstRound.roundNumber} — ${analysis.worstRound.reason}`:t('noData')}</p></div></section><section className="card"><span className="eyebrow">{t('insights')}</span><h2>{t('biggestMistakes')}</h2><ul>{analysis.topMistakes.map((x,i)=><li key={i}>{x}</li>)}</ul><h2>{t('goodDecisions')}</h2><ul>{analysis.topDecisions.map((x,i)=><li key={i}>{x}</li>)}</ul></section><section className="card"><span className="eyebrow">{t('opponent')}</span><h2>{t('patterns')}</h2><ul>{analysis.opponentPatterns.length?analysis.opponentPatterns.map((x,i)=><li key={i}>{x}</li>):<li>{t('patternMissing')}</li>}</ul></section><section className="card"><span className="eyebrow">{t('development')}</span><h2>{t('training')}</h2><div className="analysis-list">{analysis.trainingPlan.map(day=><div className="analysis-row" key={day.day}><span>{t('day')} {day.day}</span><b>{day.focus}</b></div>)}</div></section><button className="secondary" onClick={()=>void generate()} disabled={generating}>{generating?t('generating'):t('regenerate')}</button></>}<nav className="bottom-nav"><button onClick={()=>navigate('home')}><span>⌂</span><small>{t('home')}</small></button><button onClick={()=>navigate('matches')}><span>◫</span><small>{t('matches')}</small></button><button className="active"><span>◈</span><small>{t('patterns')}</small></button><button onClick={()=>navigate('home')}><span>◎</span><small>{t('profile')}</small></button></nav></main>}
